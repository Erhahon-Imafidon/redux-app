import { PostsList } from './components/index.ts';

const App = () => {
    return (
        <main className="container h-full">
            <PostsList />
        </main>
    );
};

export default App;
