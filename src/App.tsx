import Counter from './features/counter/Counter.tsx';

const App = () => {
    return (
        <main className="container min-h-screen">
            <Counter />
        </main>
    );
};

export default App;
